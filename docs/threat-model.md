# Threat model

Threats include path traversal, symlink escape, parser bombs, oversized input, hostile metadata, HTML/Markdown injection, secret leakage, action compromise, artifact substitution, TOCTOU and false maintainer claims. Controls are no-exec, exact SHA, bounded acquisition, escaping, redaction, pinned Actions, allowlisted paths, read-back and manual review.
