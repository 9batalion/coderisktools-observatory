# Operator runbook

The operator resolves a target to an exact SHA, verifies license and scope, runs safe acquisition and adapters locally, reviews normalized findings, applies policy, inspects the artifact diff, records a decision and opens a publication PR. Merge remains manual.
