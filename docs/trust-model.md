# Trust model

Trust is derived from exact SHA binding, reproducible local output, explicit tool/ruleset identity, redaction, operator decisions, manifests, checksums, protected repository paths and public correction/retraction records. Observatory does not claim the analyzed project is secure.
