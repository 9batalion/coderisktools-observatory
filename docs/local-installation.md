# Local installation

Requirements: Python 3.11+ and Git. The bootstrap has no runtime dependencies outside the standard library. Create an optional virtual environment, then run the unittest and compile commands from the README.
