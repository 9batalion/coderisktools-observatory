# Disclosure policy

Visibility classes are `PUBLIC_SUMMARY`, `MAINTAINER_ONLY`, `EMBARGOED`, `REDACTED` and `REJECTED`. Public summaries never include active secrets, credentials, personal data or operational exploit instructions.
