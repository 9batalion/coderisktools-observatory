# Methodology

A report must state what was analyzed, exact SHA, acquisition mode, adapters, versions, ruleset digest, status (`complete`, `partial` or `failed`), findings, limitations and publication decision. Missing results and scanner errors are distinct.
